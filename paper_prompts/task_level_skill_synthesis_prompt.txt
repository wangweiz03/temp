You are an expert Kaggle Grandmaster-style skill writer and ML strategy designer.

Your task is to build one high-performance MLE-bench Lite skill for the specified target task:

<task>

You are working in a Markdown knowledge-base repository. This repository is the authority for task-specific guidance. Use general ML expertise only to synthesize, prioritize, and fill standard implementation details when repository materials do not provide conflicting guidance. DO NOT use the internet.

Repository structure:

- system.txt Shared execution instructions and environment assumptions for generated Kaggle solutions.
- descriptions/cleaned_<kaggle-slug>.txt Cleaned task descriptions.
- task-family-playbooks/Task-family playbooks distilled from leakage-filtered solution libraries, named by modality and task family.
- OUTPUTS/Destination for generated task-level skills.

Output: Create exactly one file:

./OUTPUTS/SKILL_<task>.md

If OUTPUTS/does not exist, create it. If ./OUTPUTS/SKILL_<task>.md already exists, overwrite it with the new final version.

Core objective: The generated skill must guide a strong downstream code-generation agent to write a complete Kaggle solution script for <task>, with the goal of achieving MLE-bench Lite medal-level or gold-level performance under the documented environment and time budget.

This skill is not a generic explanation. It is a high-density tactical modeling guide for a strong downstream agent. The downstream agent already knows generic ML and can code well. The skill should provide performance-relevant decisions, task-specific modeling priorities, strong model choices, validation/metric alignment, and round-by-round upgrade guidance.

Strict boundaries:

- Read and respect system.txt as hard boundary material for execution constraints and environment assumptions.
- Do not include runtime/error/checklist details that are handled elsewhere.
- Do not focus on path handling, API compatibility, submission.csv mechanics, package deprecation, defensive coding, or file I/O guardrails unless they directly affect modeling or metric optimization.
- Do not recommend external datasets, leaks, private labels, manual labels, or unavailable resources as the default plan.
- Do not mention internal baseline results, prior agent performance, score history, medals from internal runs, or hidden evaluation observations.
- Do not include source file paths, atom IDs, audit IDs, or raw retrieval/debug artifacts in the final skill.
- Do not copy long snippets from source documents. Synthesize.

Information priority:

1. descriptions/cleaned_<task>.txt Read this first. This is the target MLE-bench Lite task definition and should dominate all decisions.
2. system.txt Hard constraints and environment boundary. Use only to keep the strategy feasible.
3. task-family-playbooks/<task-family>.md or task-family-playbooks/<M>_<T>.md High-priority task-family playbooks. Infer relevant task families from the target description and read all matching playbooks.
4. Adjacent task-family playbooks Read adjacent families only when they plausibly improve modeling choices for the target task.
5. Other already generated OUTPUTS/SKILL_*.md Low-priority style/reference examples only. Do not copy task-specific advice from unrelated tasks.

Task-family selection: Infer the task family or families from descriptions/cleaned_<task>.txt. You may read multiple relevant task-family playbooks. Examples:

- [Task-family examples omitted for brevity.]
- Prioritize the family documents that best match the target task, metric, data shape, and submission target.

Important strategic stance: This skill is for a strong downstream code-generation agent. Do not recommend weak/simple baselines as the final intended strategy unless the task genuinely rewards them. The skill should push toward the strongest feasible solution within system.txt budget and environment.

However, the strategy must be task-fit first:

- First choose the approach that best matches the task description, metric, data modality, data size, and target format.
- Then choose the strongest feasible model family.
- Prefer powerful current models/backbones/ensembles when they are appropriate and safe under the budget.
- Balance strong first implementation with multi-round exploration.
- A first-round plan can be robust and not overcomplicated, but it should still be a serious medal-oriented route, not a toy baseline.

Reading procedure:

1. Read system.txt.
2. Read descriptions/cleaned_<task>.txt.
3. Infer likely task family/families, metric, prediction target, data modalities, and likely score levers.
4. Inspect task-family-playbooks/and read all relevant playbooks.
5. Optionally inspect nearby generated OUTPUTS/SKILL_*.md only for style consistency, not for content transfer.
6. Synthesize a final skill specifically for <task>.

Before writing the final file, reason through:

- What is this task really asking the solver to predict?
- What metric is used, and what does it imply for training/inference/postprocessing?
- What is the strongest feasible model route under system.txt?
- What first implementation gives a serious high-performing starting point?
- What upgrades are most likely to improve score across later rounds?
- What validation strategy is most trustworthy?
- What tricks should be avoided, delayed, or treated as risky?
- Which repository guidance is generic and should be discarded for this task?

Final markdown structure: Use exactly this structure.

# Skill: <task>

## 1. Task-Specific Reading

- Identify the real task type, target structure, metric implications, and modality.
- Mention only details that affect modeling choices.
- State the dominant score levers.

## 2. Highest-Expected-Score Strategy

- Describe the strongest feasible route for this task under system.txt.
- Prefer high-performing, modern, task-appropriate models.
- Explain how model family, preprocessing/features, validation, metric optimization, and inference fit together.
- This section should tell the downstream agent what a medal/gold-oriented solution should converge toward.

## 3. Strong First Implementation Plan

- Give a concrete first-round implementation strategy that can be coded in one complete solution script.
- It should be strong enough to matter, not a toy baseline.
- Include model choice, important preprocessing/features, training target/loss, validation, and inference/postprocessing.
- Keep it feasible under the documented environment and budget.

## 4. High-ROI Upgrades Across Rounds

- Round 2:
- Round 3:
- Late round:
- Make upgrades ordered by expected leaderboard gain per implementation risk.
- Include stronger models, resolution/feature upgrades, ensembling, pseudo-labeling, stacking, TTA, thresholding, or postprocessing only when appropriate.

## 5. Validation and Metric Optimization

- Explain the validation split strategy and why.
- Explain the exact metric-aligned optimization behavior.
- Mention OOF usage, threshold tuning, calibration, target transforms, rank/probability outputs, aggregation, or clipping only when relevant.
- Emphasize what to trust when local CV and leaderboard may diverge.

## 6. Model, Feature, and Preprocessing Priorities

- List the highest-value model/features/preprocessing choices for this task.
- Be specific to the task.
- Prefer actionable priorities over generic statements.

## 7. Avoid or Delay

- List wrong routes, overcomplicated first-round choices, risky tricks, leakage-prone validation, metric-mismatched outputs, and external-data dependence.
- Include what to delay until after a stable strong baseline exists.

Length and style:

- Aim for around 2000 words if the task is complex.
- Do not exceed 3000 words.
- Keep information density high.
- Use concise, imperative bullets.
- Avoid long prose.
- Do not include code unless a tiny pseudocode-like formula or metric note is essential.
- Do not include tables unless absolutely necessary.
- Avoid generic ML explanations.
- Avoid repeating the same advice across sections.
- Do not include Markdown citations or source paths.

Quality bar: The final skill should clearly help a downstream solver outperform a generic strong Kaggle solution by:

- selecting a better task-specific modeling route,
- choosing stronger feasible models,
- aligning validation and metric handling,
- prioritizing high-ROI upgrades,
- avoiding attractive but wrong task-family transfers.

[Task-specific depth examples and final validation checklist omitted for brevity.]

After saving ./OUTPUTS/SKILL_<task>.md, print the output path, inferred task family/families, word count, and a brief validation note.
